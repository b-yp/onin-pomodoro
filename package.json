{
  "name": "onin-pomodoro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:index": "tsc && vite build",
    "build:lifecycle": "vite build --config vite.lifecycle.config.js",
    "build": "npm run build:index && npm run build:lifecycle",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "onin-sdk": "link:/Users/byp/Desktop/byp/repositories/Onin/packages/sdk"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "svelte": "^5.0.0",
    "typescript": "^5.5.0",
    "vite": "^7.3.1"
  }
}
